# VoxLoom — CPU build (default)
# For AMD ROCm GPU acceleration use the overlay:
#   docker compose -f docker-compose.yml -f docker-compose.rocm.yml up --build

services:
  voxloom:
    build: .
    container_name: voxloom
    restart: unless-stopped

    ports:
      # Host-side moved to 17600 so the dev/installed VoxLoom can keep 17493.
      # Container still listens on its native port internally.
      - "127.0.0.1:17600:17493"

    volumes:
      # Bind-mount for generated audio (customize the host path as needed)
      # Host side:      ./output/
      # Container side: /app/data/generations/
      - ./output:/app/data/generations

      # Named volume for profiles, DB, cache (persists across container restarts)
      - voxloom-data:/app/data

      # HuggingFace model cache (so models aren't re-downloaded on rebuild)
      - huggingface-cache:/home/voxloom/.cache/huggingface

    environment:
      - LOG_LEVEL=info
      - NUMBA_CACHE_DIR=/tmp/numba_cache

    networks:
      - voxloom-net

    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 8G

networks:
  voxloom-net:
    driver: bridge

volumes:
  voxloom-data:
  huggingface-cache:
